node_modules/
.next/
out/
next-env.d.ts

# environment — never commit real values
.env
.env.local
.env*.local

# Terraform state and secrets
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfstate.backup
terraform/terraform.tfvars
terraform/override.tf
terraform/override.tf.json

# act secrets (written by demo/ci/run.sh, deleted on exit but gitignore as safety net)
/tmp/act-secrets.*
act-secrets.*

# macOS
.DS_Store

# Factory progress stream (written per run by demo/lib/progress-tap.mjs)
.autofactory/run-progress.ndjson

# TypeScript incremental build info (build artifact)
tsconfig.tsbuildinfo
